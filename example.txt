# ==========================================
# COMMON / SHARED
# ==========================================
FLASK_ENV=production
# Secret keys for sessions/encryption (Required)
SECRET_KEY=your-secret-key
ENCRYPTION_KEY=your-encryption-key

# ==========================================
# SERVICE: WATCHER
# ==========================================
# Watcher DB Connection (Note the unique variable name)
WATCHER_DB_PASSWORD=your-watcher-db-password
WATCHER_DB_URL=postgresql+psycopg://watcher:your-watcher-db-password@postgres_watcher:5432/watcher

# Admin User Bootstrapping
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-admin-password
ALLOW_BOOTSTRAP_ADMIN=true

# Service Connections
NER_SERVICE_URL=http://ner:8000
LIBRETRANSLATE_URL=http://libretranslate:4000
VAULT_ROOT=/app/vault_data
ENABLE_SCHEDULER=true

# Storage (S3 Optional)
STORAGE_TYPE=local
# S3_BUCKET=my-bucket
# AWS_ACCESS_KEY_ID=...
# AWS_SECRET_ACCESS_KEY=...

# Auth Providers (Optional)
# GOOGLE_CLIENT_ID=...
# GOOGLE_CLIENT_SECRET=...

# ==========================================
# SERVICE: TELEGRAM RSS
# ==========================================
# RSS DB Connection
RSS_DB_PASSWORD=your-rss-db-password
RSS_DB_URL=postgresql+psycopg://rssuser:your-rss-db-password@postgres_rss:5432/telegram_rss

# Telegram API Credentials (REQUIRED)
MESS_BOT_API_ID=your-telegram-api-id
MESS_BOT_API_HASH=your-telegram-api-hash

# ==========================================
# SERVICE: HARVESTER & FUSEKI
# ==========================================
FUSEKI_ENDPOINT=http://fuseki:3030/knowledge-graph/update
FUSEKI_DATASET_NAME=knowledge-graph
FUSEKI_ADMIN_USER=your-fuseki-admin-user
FUSEKI_ADMIN_PASSWORD=your-fuseki-admin-password
POLL_INTERVAL=300

# ==========================================
# SERVICE: LIBRETRANSLATE
# ==========================================
LT_UPDATE_MODELS=true
LT_LOAD_ONLY=en,fr,ru,fa,ar,es,zh
